You are given a system of functions

S = {1, x^2, x^4}   on   [a, b] = [1.3, 36/10].

One can show that these functions form a Chebyshev system of functions on the given interval. In Matlab write the function

function sol = RemezSpecial(fun,a,b,tol,kmax),

that finds an element of the best uniform approximation from the approximation space S using generalized Remez algorithm for the function fun on the interval [a,b]. The tolerance tol is prescribed tolerance while kmax denotes the maximal number of steps of the algorithm.

Test the function RemezSpecial on the data:

f(x) = ln(23x/10) cos(pi*x/2),   tol = 10^(-6),   kmax = 15,

and solve the following problems:

(a) Display the coefficients of the obtained generalized approximating polynomial and enter it's constant term.
-0,26339123177866 
(b) Enter the maximum absolute value of the approximation error for the obtained generalized polynomial.
0,342146 
(c) Plot (in the same figure) the original function f along with the approximating polynomials obtained at each of the kmax iterations. If you will attach the figure to the final zip file, enter 1, otherwise enter 0. 
1